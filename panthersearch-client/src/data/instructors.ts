import type { Instructor } from '../types';

export const instructors: Instructor[] = [];
export const instructorMap = new Map<string, Instructor>();
export const mostViewedInstructors: Instructor[] = [];

